# ==============================================================================
# Auto Tender Writer — Requirements
# Core philosophy: Python is the Enforcer. LLM is the Reasoner.
# ==============================================================================

# --- Web Framework & Server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20      # Required for UploadFile / multipart form support

# --- Data Validation (our shield against malformed LLM output) ---
pydantic==2.10.4
pydantic-settings==2.7.0      # BaseSettings for config.py env-var management

# --- ORM & Database ---
sqlalchemy==2.0.36            # Async SQLAlchemy 2.0
alembic==1.14.0               # Database migrations
aiosqlite==0.20.0             # Async SQLite driver (dev / testing)
asyncpg==0.30.0               # Async PostgreSQL driver (production)

# --- Background Task Queue ---
celery==5.4.0
redis==5.2.1

# --- LLM Integration ---
litellm>=1.55.8               # Unified LLM interface (OpenAI-compatible, Kimi K2.5, etc.)
openai>=1.59.3                # OpenAI SDK (used as the wire protocol)
instructor>=1.7.0             # Enforced structured JSON extraction from LLMs via Pydantic
google-genai>=1.5.0           # Google Gemini native SDK (used when LLM_PROVIDER=gemini)

# --- PDF Processing ---
pdfplumber==0.11.4            # Primary PDF text extraction
pymupdf==1.25.1               # Fallback: PyMuPDF (fitz) for complex PDFs

# --- Security & Utilities ---
python-jose[cryptography]==3.3.0   # JWT token handling
passlib[bcrypt]==1.7.4             # Password hashing
python-dotenv==1.0.1               # .env file loading
httpx>=0.23.0                      # Async HTTP client — version controlled by litellm

# --- Observability ---
loguru==0.7.3                      # Structured logging
